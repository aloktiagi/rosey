{
  "name": "rosey-baileys",
  "version": "0.1.0",
  "private": true,
  "description": "Baileys-based WhatsApp adapter for Rosey. Runs as a sidecar to the Python Quart server: receives WhatsApp messages (including group messages, which Cloud API gates behind OBA), forwards them to Python via loopback HTTP, and accepts outbound send requests from Python via its own loopback HTTP server.",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@whiskeysockets/baileys": "^6.7.18",
    "qrcode-terminal": "^0.12.0",
    "pino": "^9.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
